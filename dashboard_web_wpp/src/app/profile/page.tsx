import { Header, ProfileHeader, ProfileInfo } from '@/components';

const page = () => {
  return (
    <div className='mx-8 px-8 w-full'>
      <ProfileInfo/>
    </div>
  );
};

export default page;
